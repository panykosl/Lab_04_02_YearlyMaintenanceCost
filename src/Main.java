public class Main {
    public static void main(String[] args) {
        double springCost = 30.0;
        double summerCost = 35.0;
        double fallCost = 28.0;
        double winterCost = 40.0;
        double totalCost = 0.0;
        totalCost = springCost + summerCost + fallCost + winterCost;
        System.out.println("The spring cost is " + springCost + " plus the summer cost " + summerCost + " plus the fall cost " + fallCost + " plus the winter cost" + winterCost + " equals " +totalCost);
    }
}